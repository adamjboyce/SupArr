# =============================================================================
# Machine 2: *arr Stack — Environment Configuration
# =============================================================================
# Copy to .env and fill in your values before running init script.
# API keys auto-populated by init script on first run.
# =============================================================================

# User/Group IDs (run `id` on host)
PUID=1000
PGID=1000

# Timezone
TZ=America/Chicago

# Paths
MEDIA_ROOT=/mnt/media
DOWNLOADS_ROOT=/mnt/downloads       # Local NVMe preferred
APPDATA=/opt/arr-stack

# NAS — for automatic NFS mount setup
NAS_IP=
NAS_MEDIA_EXPORT=/volume1/media
NAS_DOWNLOADS_EXPORT=/volume1/downloads
# Optional: separate NFS share for backups (Immich photos, Syncthing, phone backup)
# If not set, Immich/Syncthing use paths under MEDIA_ROOT
# NAS_BACKUPS_EXPORT=/volume1/backups

# NordVPN (via Gluetun)
NORD_VPN_TYPE=openvpn                # or 'wireguard' for NordLynx
NORD_USER=your_nord_service_email
NORD_PASS=your_nord_service_password
# For WireGuard/NordLynx (faster):
# NORD_WIREGUARD_KEY=your_nordlynx_private_key
NORD_COUNTRY=United States
NORD_CITY=

# qBittorrent — set a custom password (default: adminadmin)
QBIT_PASSWORD=

# qBittorrent queue limits (optional — sensible defaults in init script)
# QBIT_MAX_DOWNLOADS=30
# QBIT_MAX_TORRENTS=50
# QBIT_MAX_UPLOADS=15

# Local network — Gluetun needs this for LAN access to VPN'd containers
LOCAL_SUBNET=192.168.1.0/24

# Tailscale
TAILSCALE_AUTH_KEY=tskey-auth-XXXXXXXXXXXXXXXXXXXX

# *arr API Keys — AUTO-POPULATED by init script on first run
# Leave blank; the script reads them from container config files
RADARR_API_KEY=
SONARR_API_KEY=
LIDARR_API_KEY=
PROWLARR_API_KEY=
BAZARR_API_KEY=
BOOKSHELF_API_KEY=
WHISPARR_API_KEY=

# SABnzbd — set after running SABnzbd setup wizard
SABNZBD_API_KEY=

# Notifiarr — https://notifiarr.com
NOTIFIARR_API_KEY=

# Plex integration — *arr → Plex library scan on import + import lists
# Set these if you want auto content discovery and Plex Connect notifications
PLEX_TOKEN=
PLEX_IP=
TMDB_API_KEY=
# Trakt access token for import lists (auto-populated by setup.sh)
TRAKT_ACCESS_TOKEN=

# Immich — phone photo/video backup
IMMICH_DB_PASSWORD=

# Media Library Migration (optional)
# Set MIGRATE_LIBRARY=true to mount and import an existing media library
MIGRATE_LIBRARY=false
MIGRATE_SOURCE=
MIGRATE_NAS_EXPORT=

# Discord notifications (optional)
# Get webhook URL: Server Settings → Integrations → Webhooks → New Webhook
DISCORD_WEBHOOK_URL=

# Watchtower notifications — auto-derived from DISCORD_WEBHOOK_URL
# Only set manually if you want a DIFFERENT notification target for updates
WATCHTOWER_NOTIFICATION_URL=
